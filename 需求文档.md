# 用户权限功能需求文档

## 1. 背景

当前系统中的简历管理、面试记录等数据没有按照登录用户进行隔离。为支持多用户使用，需要新增账户登录认证和角色权限管理能力，保证普通用户只能访问自己创建的数据，管理员可以访问所有用户的数据。

## 2. 目标

- 新增用户账号体系，支持账号密码登录。
- 新增角色体系，区分普通用户和管理员。
- 登录成功后，前端进入简历管理页面。
- 后端接口根据当前登录用户做数据隔离。
- 普通用户只能查看、创建、修改、删除自己的简历和面试记录。
- 管理员可以查看和管理所有用户的简历和面试记录。
- 系统设置中的模型 Provider 支持按用户隔离，普通用户可以维护自己的 Provider 和默认模型。
- 前端支持登录状态保持、路由守卫和退出登录。

## 3. 角色定义

| 角色 | 说明 | 权限 |
|------|------|------|
| 普通用户 | 默认注册或创建的用户角色 | 只能访问自己创建的简历管理、面试记录等数据 |
| 管理员 | 系统管理角色 | 可以访问所有用户创建的简历管理、面试记录等数据 |

## 4. 后端需求

### 4.1 用户账号表

新增用户账号信息表，用于管理系统登录账号。

建议字段：

| 字段 | 说明 |
|------|------|
| id | 用户 ID |
| username | 登录账号，唯一 |
| password | 加密后的密码 |
| nickname | 用户昵称 |
| enabled | 是否启用 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

密码必须加密存储，禁止明文保存。

### 4.2 角色信息表

新增角色信息表，用于管理系统角色。

建议字段：

| 字段 | 说明 |
|------|------|
| id | 角色 ID |
| code | 角色编码，例如 USER、ADMIN |
| name | 角色名称，例如普通用户、管理员 |
| description | 角色描述 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

### 4.3 用户角色关系

用户和角色建议通过用户角色关联表维护，方便后续扩展一个用户多个角色的场景。

建议字段：

| 字段 | 说明 |
|------|------|
| id | 主键 ID |
| user_id | 用户 ID |
| role_id | 角色 ID |

### 4.4 登录认证

后端新增登录接口：

```text
POST /api/auth/login
```

请求参数：

```json
{
  "username": "admin",
  "password": "123456"
}
```

响应结果：

```json
{
  "token": "jwt-token",
  "user": {
    "id": 1,
    "username": "admin",
    "nickname": "管理员",
    "roles": ["ADMIN"]
  }
}
```

认证方案建议使用 JWT：

- 登录成功后返回 Token。
- 前端后续请求在 Header 中携带 Token。
- 后端通过 Token 解析当前登录用户。
- 未登录访问受保护接口时返回未认证错误。

请求头格式：

```text
Authorization: Bearer ${token}
```

### 4.5 数据隔离

需要对简历管理和面试记录相关数据增加用户归属字段。

建议在以下业务表中增加 `user_id` 字段：

- resumes
- resume_analyses
- interview_sessions
- interview_answers

普通用户访问数据时，所有查询、详情、修改、删除、导出等操作都必须带上当前登录用户条件。

示例：

```text
普通用户查询简历列表：
where user_id = 当前登录用户 ID
```

管理员访问数据时，不限制 `user_id`，可以查看所有用户创建的数据。

示例：

```text
管理员查询简历列表：
查询所有用户数据
```

### 4.6 模型 Provider 隔离

系统设置中的模型 Provider 需要支持多用户使用。

设计规则：

- 管理员管理全局 Provider。
- 普通用户管理自己的 Provider。
- 普通用户不能查看、编辑、删除其他用户创建的 Provider。
- 普通用户和管理员可以创建同名 Provider，例如都叫 `deepseek`。
- 为避免数据库主键冲突，后端需要保存内部唯一 Provider ID。
- 前端仍展示用户填写的 Provider ID，不展示内部前缀。
- 普通用户可以设置自己的默认文字模型和默认向量模型。
- 普通用户默认模型不能覆盖管理员默认模型。
- 管理员默认模型不能覆盖普通用户默认模型。

建议在 Provider 表中增加以下字段：

| 字段 | 说明 |
|------|------|
| owner_user_id | Provider 所属用户 ID，管理员/全局 Provider 可为空 |
| display_id | 前端展示的 Provider ID |

内部 Provider ID 规则示例：

```text
管理员 Provider:
dashscope

普通用户 12 的 Provider:
u12:deepseek

普通用户 18 的 Provider:
u18:deepseek
```

前端展示时仍显示：

```text
deepseek
```

普通用户默认模型需要单独存储，建议新增用户模型设置表。

建议字段：

| 字段 | 说明 |
|------|------|
| user_id | 用户 ID |
| default_chat_provider_id | 当前用户默认文字模型 Provider 内部 ID |
| default_embedding_provider_id | 当前用户默认向量模型 Provider 内部 ID |
| created_at | 创建时间 |
| updated_at | 更新时间 |

管理员默认模型继续使用全局设置表。

```text
管理员默认模型：llm_global_setting
普通用户默认模型：llm_user_setting
```

默认模型解析规则：

- 管理员调用依赖默认模型的能力时，使用 `llm_global_setting` 中的全局默认模型。
- 普通用户调用依赖默认模型的能力时，使用 `llm_user_setting` 中当前用户自己的默认模型。
- 按数据归属用户执行的异步任务，例如简历异步分析，需要根据业务数据的 `user_id` 判断角色：
  - 如果 `user_id` 对应管理员，使用全局默认模型。
  - 如果 `user_id` 对应普通用户，使用该普通用户自己的默认模型。
- 普通用户不能因为没有设置默认模型而回退使用管理员的全局默认模型。

普通用户没有设置默认模型时：

- 不应自动使用管理员默认模型。
- 调用需要默认模型的能力时，应提示用户先到系统设置中新增 Provider 并设为默认模型。

异步任务规则：

- 面试会话创建时，应保存当时实际使用的 Provider 内部 ID。
- 后续异步评估继续使用会话中保存的 Provider，不受管理员或用户后续修改默认模型影响。

### 4.7 权限控制规则

- 未登录用户不能访问简历管理、面试记录等业务接口。
- 普通用户只能访问自己的数据。
- 管理员可以访问所有用户的数据。
- 普通用户访问其他用户数据时，返回无权限或资源不存在。
- 后端权限判断必须放在 Service 层或统一权限组件中，不能只依赖前端控制。
- 普通用户可以访问系统设置中的模型 Provider 管理，但只能操作自己的 Provider。
- 全局语音 ASR/TTS 配置属于系统级配置，默认仅管理员可管理。

## 5. 前端需求

### 5.1 登录页面

新增登录页面。

页面内容：

- 系统名称或 Logo。
- 账号输入框。
- 密码输入框。
- 登录按钮。

交互规则：

- 用户输入账号和密码。
- 点击登录按钮后调用后端登录接口。
- 登录成功后保存 Token。
- 登录成功后跳转到简历管理页面。
- 登录失败时提示错误信息。

登录成功跳转地址：

```text
/history
```

### 5.2 请求鉴权

前端需要在统一请求封装中增加 Token 处理：

- 登录成功后保存 Token 到 cookie。
- 每次调用后端接口时自动添加 `Authorization` 请求头。
- Token 失效或未登录时跳转到登录页面。
- 使用原生 `fetch` 的 SSE 请求也需要携带 Token。
- 使用单独 axios 的文件下载请求也需要携带 Token。

Token cookie 名称：

```text
interview_auth_token
```

### 5.3 路由权限

需要对业务页面增加登录校验。

受保护页面包括：

- 简历管理
- 简历详情
- 面试记录
- 面试详情
- 模拟面试
- 知识库管理
- 问答助手
- 面试日程
- 系统设置

未登录访问这些页面时，自动跳转到登录页面。

### 5.4 退出登录

左侧菜单顶部品牌区域增加退出登录图标。

交互规则：

- 点击退出登录图标。
- 清除 `interview_auth_token` cookie。
- 跳转到登录页面。
- 退出后再次访问业务页面时，路由守卫应跳转到登录页面。

### 5.5 系统设置页面

系统设置页面面向管理员和普通用户开放。

普通用户可见能力：

- 查看自己的 Provider 列表。
- 新增自己的 Provider。
- 编辑自己的 Provider。
- 删除自己的 Provider。
- 测试自己的 Provider 连通性。
- 设置自己的默认文字模型。
- 设置自己的默认向量模型。

管理员可见能力：

- 查看和管理全局 Provider。
- 设置全局默认文字模型。
- 设置全局默认向量模型。
- 管理全局语音 ASR/TTS 配置。

## 6. 验收标准

- 用户可以通过账号密码登录系统。
- 登录成功后可以进入简历管理页面。
- 未登录用户访问简历管理页面时会跳转到登录页面。
- 点击退出登录后会清除 Token 并跳转登录页。
- 普通用户只能看到自己创建的简历数据。
- 普通用户只能看到自己创建的面试记录。
- 普通用户不能访问其他用户的简历详情和面试记录详情。
- 管理员可以看到所有用户创建的简历和面试记录。
- 普通用户只能看到和管理自己的 Provider。
- 不同普通用户可以创建同名 Provider，互不冲突。
- 普通用户设置自己的默认模型不会影响管理员默认模型。
- 管理员设置全局默认模型不会影响普通用户默认模型。
- 管理员配置全局默认模型后，管理员发起的简历分析等异步任务应可以正常使用全局默认模型。
- 普通用户没有默认模型时，调用依赖默认模型的能力应提示先配置 Provider。
- 后端接口不能因为前端隐藏入口而绕过权限控制。

## 7. 实现归档

### 7.1 已完成后端改动

- 新增 Spring Security + JWT 登录认证。
- 新增账号、角色、用户角色关系模块。
- 新增默认账号：
  - `admin / admin123`
  - `user / user123`
- 新增当前登录用户解析能力。
- 简历表增加 `user_id`。
- 面试会话表增加 `user_id`。
- 简历列表、详情、删除、分析查询增加用户隔离。
- 面试会话列表、详情、删除、创建、继续面试增加用户隔离。
- Provider 表增加 `owner_user_id` 和 `display_id`。
- 新增用户模型设置表 `llm_user_setting`。
- Provider 管理改为管理员全局、普通用户个人隔离。
- 普通用户默认模型与管理员默认模型分开保存。
- 默认模型解析增加角色判断：管理员使用全局默认模型，普通用户使用个人默认模型。
- 简历异步分析按简历所属用户解析默认模型，管理员简历使用全局默认模型，普通用户简历使用个人默认模型。
- 面试会话保存实际使用的 Provider 内部 ID，供异步评估继续使用。
- Security 异常统一返回项目 `Result.error(...)` 格式。

### 7.2 已完成前端改动

- 新增登录页面。
- 登录成功后将 Token 保存到 cookie。
- axios 统一请求封装自动携带 Token。
- SSE 请求和文件下载请求补充 Token。
- 增加路由守卫，未登录访问业务页面跳转登录页。
- 左侧菜单顶部增加退出登录图标。
- 退出登录清除 cookie 并跳转登录页。
- 系统设置页面对普通用户开放 Provider 管理。
- 普通用户系统设置页面隐藏全局 ASR/TTS 语音配置。

### 7.3 验证记录

- 后端编译命令：`./gradlew :app:compileJava`
- 前端构建命令：`pnpm build`
- 以上命令已在当前开发环境验证通过。

### 7.4 注意事项

- 开发环境 `spring.jpa.hibernate.ddl-auto=update` 时，新增字段和新表通常会自动创建。
- 如果历史数据库存在旧唯一索引，例如简历 `file_hash` 全局唯一，需要人工检查并调整为按用户维度唯一。
- 旧 Provider 数据没有 `owner_user_id` 时视为管理员/全局 Provider。
- 普通用户 Provider 使用内部 ID 防冲突，前端不展示内部前缀。

## 8. 后续扩展

后续可以继续扩展以下能力：

- 用户注册。
- 修改密码。
- 找回密码。
- 用户禁用和启用。
- 管理员用户管理页面。
- 更细粒度的菜单权限和接口权限。
- 操作日志审计。
- 普通用户语音 ASR/TTS 独立配置。
