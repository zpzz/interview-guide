# InterviewGuide 项目面试学习文档

## 1. 文档说明

这份文档基于当前项目代码整理，目标不是泛泛讲技术，而是帮助你围绕这个项目准备全栈面试。

学习方式建议：

1. 先掌握每个模块“做了什么”。
2. 再掌握“为什么这么设计”。
3. 最后练习“如果面试官追问，你怎么展开”。

本文尽量按下面这个结构组织：

- 知识点
- 面试官可能怎么问
- 建议回答思路
- 本项目中的具体示例
- 延伸追问

---

## 2. 项目整体介绍

### 2.1 项目定位

`InterviewGuide` 是一个智能 AI 面试平台，覆盖：

- 简历上传、解析、评分
- 文字模拟面试
- 语音模拟面试
- 知识库管理与 RAG 问答
- 面试日程管理
- 多 LLM Provider 配置管理
- 后台用户与权限管理

### 2.2 技术栈总览

后端：

- `Spring Boot 4.0`
- `Java 21`
- `Spring AI 2.0`
- `Spring Security`
- `Spring Data JPA`
- `PostgreSQL + pgvector`
- `Redis + Redisson`
- `Redis Stream`
- `WebSocket`
- `MapStruct`
- `Apache Tika`
- `iText 8`

前端：

- `React 18`
- `TypeScript`
- `Vite`
- `TailwindCSS 4`
- `Axios`
- `React Router`
- `Framer Motion`
- `React Big Calendar`
- `Recharts`

### 2.3 一句话项目亮点

如果面试时只能先讲 30 秒，可以这样说：

> 这是一个基于 Spring Boot 4 + Spring AI + React 的智能面试平台，核心能力包括简历分析、模拟面试、语音面试和 RAG 知识库问答。我在项目里重点解决了多模型接入、结构化输出稳定性、Redis Stream 异步任务、JWT 认证、向量检索和前后端统一错误处理等问题。

---

## 3. 系统架构设计

## 3.1 分层架构

项目采用典型的：

`Controller -> Service -> Repository`

并引入基础设施层：

- `RedisService`
- `PdfExportService`
- `FileStorageService`
- `InterviewSessionCache`

### 面试官可能怎么问

`你这个项目为什么要做分层？`

### 建议回答思路

分层的核心价值有三点：

1. 职责隔离，Controller 不写业务逻辑。
2. Service 层便于复用和事务控制。
3. Repository 只负责数据访问，降低耦合。

### 本项目示例

登录接口中：

- [AuthController](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/modules/account/controller/AuthController.java) 只负责接收 `LoginRequest`
- [AuthService](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/modules/account/service/AuthService.java) 负责校验账号、密码、角色并生成 JWT
- `UserRepository` 负责查库

示例代码逻辑：

```java
@PostMapping("/api/auth/login")
public Result<AuthResponse> login(@RequestBody @Valid LoginRequest request) {
  return Result.success(authService.login(request));
}
```

这就是很标准的“Controller 只路由和委托”。

### 延伸追问

- Service 层为什么适合放事务？
- Repository 层为什么不应该写业务编排？

---

## 3.2 模块化设计

后端按业务模块拆分：

- `account`
- `resume`
- `interview`
- `voiceinterview`
- `knowledgebase`
- `interviewschedule`
- `resumemaker`
- `llmprovider`

### 面试官可能怎么问

`为什么不是按 controller/service/repository 横向拆目录，而是按业务模块拆？`

### 建议回答思路

按业务拆分更适合中大型项目，因为一个模块内的控制器、服务、实体、DTO 更集中，维护成本更低。否则会出现：

- 所有 Controller 放一起，文件爆炸
- 新人理解一个功能时要跨很多目录
- 未来拆微服务难度更大

### 本项目示例

比如语音面试相关代码都在：

- `modules/voiceinterview/controller`
- `modules/voiceinterview/service`
- `modules/voiceinterview/repository`
- `modules/voiceinterview/model`
- `modules/voiceinterview/dto`

这样读代码时上下文更完整。

---

## 4. 后端基础能力

## 4.1 统一响应封装 Result<T>

项目使用统一响应体 `Result<T>`，并约定：

- 成功：`code = 200`
- 失败：依然返回 HTTP 200，但业务码不是 200

对应实现见：

- [Result.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/common/result/Result.java)

### 面试官可能怎么问

`为什么项目里要统一返回 Result<T>？`

### 建议回答思路

统一响应的好处：

1. 前端处理简单，成功失败逻辑统一。
2. 便于做全局异常处理。
3. 后期增加 traceId、分页信息、扩展字段更方便。

### 本项目示例

```java
public static <T> Result<T> success(T data) {
  return new Result<>(200, "success", data);
}
```

错误也统一封装：

```java
public static <T> Result<T> error(Integer code, String message) {
  return new Result<>(code, message, null);
}
```

### 可以怎么讲亮点

这个项目甚至将业务异常也统一包装成 HTTP 200，这样前端只需要认 `Result.code`，不需要在很多地方区分 4xx/5xx。

### 风险与思考

这种做法的缺点是：

- 不符合严格 REST 语义
- 对网关、监控平台的 HTTP 状态码分析不够友好

如果在企业项目中，可以根据场景选择：

- 面向前端 BFF 可统一 200
- 面向外部开放 API 更建议遵循标准 HTTP 状态码

---

## 4.2 全局异常处理

对应实现：

- [GlobalExceptionHandler.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/common/exception/GlobalExceptionHandler.java)

### 面试官可能怎么问

`你们项目异常是怎么统一处理的？`

### 建议回答思路

可以从三层讲：

1. 业务层用 `BusinessException`
2. 参数校验异常用全局异常处理器兜底
3. AI 服务异常、上传异常、未知异常统一转换成业务码

### 本项目示例

业务异常：

```java
@ExceptionHandler(BusinessException.class)
@ResponseStatus(HttpStatus.OK)
public Result<Void> handleBusinessException(BusinessException e) {
  return Result.error(e.getCode(), e.getMessage());
}
```

参数校验异常：

```java
@ExceptionHandler(MethodArgumentNotValidException.class)
public Result<Void> handleValidationException(MethodArgumentNotValidException e) {
  String message = e.getBindingResult().getFieldErrors().stream()
      .map(FieldError::getDefaultMessage)
      .collect(Collectors.joining(", "));
  return Result.error(ErrorCode.BAD_REQUEST, message);
}
```

AI 网络异常：

```java
@ExceptionHandler(ResourceAccessException.class)
public Result<Void> handleResourceAccessException(ResourceAccessException e) {
  return Result.error(ErrorCode.AI_SERVICE_UNAVAILABLE, "AI服务暂时不可用，请稍后重试");
}
```

### 你可以这样回答

> 我们没有在 Controller 里到处 try-catch，而是通过 `@RestControllerAdvice` 做集中式异常转换。业务逻辑中统一抛 `BusinessException`，参数校验、网络异常、上传异常、AI 超时等都在全局层统一转成 `Result.error(...)`，这样前端拿到的是稳定的错误协议。

---

## 4.3 错误码体系设计

项目把错误码分域管理：

- `1xxx` 通用错误
- `2xxx` 简历
- `3xxx` 面试
- `4xxx` 存储
- `5xxx` 导出
- `6xxx` 知识库
- `7xxx` AI 服务
- `8xxx` 限流
- `9xxx` 面试日程
- `10xxx` 语音面试

### 面试官可能怎么问

`为什么要分域错误码？直接写字符串不行吗？`

### 建议回答思路

分域错误码的价值：

1. 日志检索方便
2. 前后端约定更稳定
3. 运维排障更快
4. 便于统计某类问题的发生频率

### 示例

假设知识库向量化失败：

```java
throw new BusinessException(
  ErrorCode.KNOWLEDGE_BASE_VECTORIZATION_FAILED,
  "向量化知识库失败: " + e.getMessage()
);
```

面试时可以进一步说，如果以后对接监控告警，可以直接按错误码前缀聚合。

---

## 5. Spring Boot 与 Java 21

## 5.1 Spring Boot 4 的使用

这个项目基于 `Spring Boot 4.0`。

### 面试官可能怎么问

`Spring Boot 在这个项目里给你解决了什么问题？`

### 建议回答思路

可以从自动配置、Starter、生态整合来讲：

1. 用 Starter 快速集成 Web、JPA、Validation、WebSocket、Security
2. 用自动配置降低样板代码
3. 与 Spring AI、SpringDoc、Redisson 等生态无缝集成

### 本项目示例

[build.gradle](/Users/adian/demo/interview-guide/app/build.gradle) 中直接通过 starter 引入：

```gradle
implementation 'org.springframework.boot:spring-boot-starter-webmvc'
implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
implementation 'org.springframework.boot:spring-boot-starter-security'
implementation 'org.springframework.boot:spring-boot-starter-websocket'
```

启动类：

- [App.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/App.java)

```java
@EnableScheduling
@SpringBootApplication(exclude = {
  OpenAiAudioSpeechAutoConfiguration.class,
  OpenAiAudioTranscriptionAutoConfiguration.class,
  OpenAiChatAutoConfiguration.class
})
```

### 这里的面试亮点

项目排除了部分 OpenAI 自动配置，说明不是完全依赖默认自动装配，而是为了做多 Provider 动态管理，自己接管了 ChatModel 和 EmbeddingModel 的创建逻辑。

---

## 5.2 Java 21 的使用价值

### 面试官可能怎么问

`为什么项目选择 Java 21？`

### 建议回答思路

Java 21 是 LTS 版本，主要价值：

- 更现代的语言特性
- 更稳定的虚拟线程生态
- 更适合 IO 密集型场景

### 本项目中体现的现代写法

`switch` 表达式：

```java
return switch (unit) {
  case MILLISECONDS -> interval;
  case SECONDS -> interval * 1000;
  case MINUTES -> interval * 60 * 1000;
  case HOURS -> interval * 3600 * 1000;
  case DAYS -> interval * 86400 * 1000;
};
```

这是 [RateLimitAspect.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/common/aspect/RateLimitAspect.java) 里的代码风格。

`record` 作为请求体：

```java
public record QueryRequest(
  @NotEmpty(message = "至少选择一个知识库")
  List<Long> knowledgeBaseIds,
  @NotBlank(message = "问题不能为空")
  String question
) {}
```

见 [QueryRequest.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/modules/knowledgebase/model/QueryRequest.java)。

### 面试可延伸

如果面试官继续问虚拟线程，可以说：

> 这个项目技术选型上是 Java 21，适合未来在 AI 调用、文件处理、RAG 检索等 IO 密集场景中引入虚拟线程，减少平台线程阻塞带来的资源浪费。

---

## 6. 安全与认证

## 6.1 JWT 认证

对应实现：

- [SecurityConfig.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/common/config/SecurityConfig.java)
- [JwtAuthenticationFilter.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/common/auth/JwtAuthenticationFilter.java)
- [AuthService.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/modules/account/service/AuthService.java)

### 面试官可能怎么问

`你们项目登录认证是怎么做的？`

### 建议回答思路

按登录链路讲：

1. 用户调用 `/api/auth/login`
2. 后端查库校验用户名密码
3. 生成 JWT，携带用户 id 和角色
4. 前端后续请求通过 `Authorization: Bearer xxx` 传 token
5. `JwtAuthenticationFilter` 解析 token 并写入 `SecurityContext`

### 本项目示例

登录接口：

```java
@PostMapping("/api/auth/login")
public Result<AuthResponse> login(@RequestBody @Valid LoginRequest request) {
  return Result.success(authService.login(request));
}
```

登录业务：

```java
UserEntity user = userRepository.findByUsernameWithRoles(request.username())
    .orElseThrow(() -> new BusinessException(ErrorCode.USERNAME_OR_PASSWORD_INVALID));

if (!passwordEncoder.matches(request.password(), user.getPassword())) {
  throw new BusinessException(ErrorCode.USERNAME_OR_PASSWORD_INVALID);
}
```

过滤器中解析 token：

```java
CurrentUser currentUser = jwtService.parseToken(token);
UsernamePasswordAuthenticationToken authentication =
    new UsernamePasswordAuthenticationToken(currentUser, null, authorities);
SecurityContextHolder.getContext().setAuthentication(authentication);
```

### 你可以这样概括

> 我们采用无状态 JWT 认证，登录成功后由服务端签发 token，请求进入时通过自定义 `OncePerRequestFilter` 解析 token，把用户信息和角色注入 Spring Security 上下文，从而实现后续接口鉴权。

---

## 6.2 Spring Security 配置

### 面试官可能怎么问

`Spring Security 在项目里具体做了什么？`

### 本项目示例

```java
return http
  .csrf(AbstractHttpConfigurer::disable)
  .sessionManagement(session -> session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
  .authorizeHttpRequests(auth -> auth
      .requestMatchers("/api/auth/login").permitAll()
      .anyRequest().authenticated()
  )
  .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class)
  .build();
```

### 关键知识点

- `STATELESS` 表示不走 Session
- `permitAll()` 放行登录接口和 Swagger
- 自定义 JWT 过滤器加在用户名密码过滤器之前
- 自定义 `authenticationEntryPoint` 和 `accessDeniedHandler` 统一错误输出

### 面试延伸

可以顺带说清楚 `401` 和 `403` 的区别：

- `401` 未认证
- `403` 已认证但无权限

---

## 6.3 密码加密

### 面试官可能怎么问

`为什么不能明文存密码？你用了什么方案？`

### 本项目示例

```java
@Bean
public PasswordEncoder passwordEncoder() {
  return new BCryptPasswordEncoder();
}
```

登录时校验：

```java
passwordEncoder.matches(request.password(), user.getPassword())
```

### 回答要点

- 明文存储有严重泄露风险
- `BCrypt` 自带盐值和强哈希
- 即使数据库泄漏，也不能直接反推原密码

---

## 7. 参数校验与 DTO 设计

## 7.1 为什么要用 Request/DTO/Response 分层

### 面试官可能怎么问

`为什么不直接把 Entity 返回前端？`

### 建议回答思路

原因有四个：

1. 避免暴露数据库字段
2. 避免返回敏感信息
3. 响应结构可以按前端需要定制
4. 领域模型和接口模型解耦

### 本项目示例

登录返回的是 `AuthResponse` 和 `UserDTO`，不是 `UserEntity`。

这意味着你不会把数据库里的密码、状态位、创建时间原样暴露给前端。

---

## 7.2 Jakarta Validation

### 面试官可能怎么问

`接口参数校验怎么做的？`

### 本项目示例

```java
public record QueryRequest(
  @NotEmpty(message = "至少选择一个知识库")
  List<Long> knowledgeBaseIds,
  @NotBlank(message = "问题不能为空")
  String question
) {}
```

Controller 使用：

```java
public Result<AuthResponse> login(@RequestBody @Valid LoginRequest request)
```

### 回答要点

- 用注解声明约束
- 用 `@Valid` 触发校验
- 校验失败进入全局异常处理

### 延伸问题

- `@Validated` 和 `@Valid` 区别是什么？
- 如果需要分组校验怎么做？

---

## 8. 限流设计

## 8.1 自定义 @RateLimit 注解

对应实现：

- [RateLimit.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/common/annotation/RateLimit.java)
- [RateLimitAspect.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/common/aspect/RateLimitAspect.java)

### 面试官可能怎么问

`你们项目限流怎么做的？为什么没直接用网关限流？`

### 建议回答思路

项目选择了应用层限流，原因是：

1. 某些接口的业务维度更细，比如按用户、按 IP、按全局
2. 需要和业务代码更紧密地绑定
3. 某些接口需要限流后走降级逻辑

### 本项目示例

注解支持重复标注：

```java
@RateLimit(dimension = RateLimit.Dimension.GLOBAL, count = 10)
@RateLimit(dimension = RateLimit.Dimension.IP, count = 10)
public Result<QueryResponse> queryKnowledgeBase(...) { ... }
```

这个设计的含义是：

- 同一个方法可以同时有多个维度的限流规则
- 所有规则都通过，请求才放行

### AOP 处理逻辑

切面会：

1. 读取方法上的所有 `@RateLimit`
2. 根据维度生成 Redis Key
3. 逐条执行 Lua 脚本
4. 任意一条失败就拒绝请求

核心代码思路：

```java
RateLimit[] rules = method.getAnnotationsByType(RateLimit.class);
for (RateLimit rule : rules) {
  Long result = executeRateLimitScript(key, nowMs, requestId, intervalMs, rule.count());
  if (result == null || result == 0) {
    return handleRateLimitExceeded(joinPoint, rule, key);
  }
}
```

### 为什么用 Lua

如果只用普通 Redis 命令，会有并发一致性问题。Lua 脚本可以把：

- 读取计数
- 判断窗口
- 更新计数

做成原子操作。

### Redis Key 设计亮点

```java
String hashTag = "{" + className + ":" + methodName + "}";
String keyPrefix = "ratelimit:" + hashTag;
```

好处：

- 不同接口隔离
- Redis Cluster 下利用 hash tag 让相关 key 落同槽

### 降级设计

如果注解里配置了 `fallback`，限流后可以不直接抛异常，而是执行兜底方法。

这是比“单纯拒绝请求”更灵活的地方。

---

## 9. Redis 的使用

## 9.1 Redis 在项目中的作用

### 面试官可能怎么问

`Redis 在这个项目里主要承担什么职责？`

### 建议回答思路

这个项目里 Redis 不只是缓存，还承担：

- 限流
- 异步任务队列
- 会话缓存

### 本项目示例

1. 限流：AOP + Lua
2. 异步：Redis Stream
3. 语音会话缓存：`voice:interview:session:{id}`

在 [VoiceInterviewService.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/modules/voiceinterview/service/VoiceInterviewService.java) 中会先查缓存：

```java
RBucket<VoiceInterviewSessionEntity> bucket = redissonClient.getBucket(cacheKey);
VoiceInterviewSessionEntity cached = bucket.get();
if (cached != null) {
  return cached;
}
```

### 可以这样回答

> Redis 在这个项目中一共承担了三类角色：高频读缓存、分布式限流状态存储、异步任务消息中间件。这样避免了把所有压力都打到数据库上。

---

## 9.2 Redisson 的优势

### 面试官可能怎么问

`为什么选 Redisson？`

### 回答思路

相比直接用 `StringRedisTemplate`，Redisson 的优势是：

- 提供更丰富的分布式对象抽象
- Stream、Bucket、Lock 等 API 更面向对象
- 适合复杂 Redis 场景

### 本项目示例

限流脚本：

```java
rScript = redissonClient.getScript(StringCodec.INSTANCE);
luaScriptSha = rScript.scriptLoad(LUA_SCRIPT);
```

会话缓存：

```java
RBucket<VoiceInterviewSessionEntity> bucket = redissonClient.getBucket(cacheKey);
```

---

## 10. Redis Stream 异步任务设计

## 10.1 为什么要异步

### 面试官可能怎么问

`简历分析、知识库向量化为什么不直接同步做？`

### 建议回答思路

这些任务的共同特征：

- 耗时长
- 可能涉及 AI 调用
- 可能涉及文件解析、向量化
- 用户不需要立即拿到最终结果

如果同步做，会导致：

- 请求耗时过长
- 用户体验差
- 服务线程被长期占用

### 本项目示例

项目把以下任务放入 Redis Stream：

- 知识库向量化
- 简历分析
- 面试评估

---

## 10.2 抽象模板设计

对应实现：

- [AbstractStreamProducer.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/common/async/AbstractStreamProducer.java)
- [AbstractStreamConsumer.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/common/async/AbstractStreamConsumer.java)

### 面试官可能怎么问

`你们的异步任务为什么要抽象成模板基类？`

### 建议回答思路

因为不同异步任务虽然业务不同，但流程很像：

1. 发消息
2. 消费消息
3. 解析 payload
4. 标记处理中
5. 执行业务
6. 成功完成或失败重试
7. ACK 消息

把公共骨架抽出来，可以减少重复代码。

### 生产者示例

```java
protected void sendTask(T payload) {
  String messageId = redisService.streamAdd(
      streamKey(),
      buildMessage(payload),
      AsyncTaskStreamConstants.STREAM_MAX_LEN
  );
}
```

### 消费者示例

```java
protected abstract void markProcessing(T payload);
protected abstract void processBusiness(T payload);
protected abstract void markCompleted(T payload);
protected abstract void markFailed(T payload, String error);
protected abstract void retryMessage(T payload, int retryCount);
```

### 这是典型什么设计模式

这是典型的模板方法模式。

公共流程写在抽象父类里，可变步骤交给子类实现。

---

## 10.3 重试机制

### 面试官可能怎么问

`异步任务失败了怎么办？`

### 本项目示例

```java
if (retryCount < AsyncTaskStreamConstants.MAX_RETRY_COUNT) {
  retryMessage(payload, retryCount + 1);
} else {
  markFailed(payload, truncateError(...));
}
ackMessage(messageId);
```

### 回答要点

- 最多重试 3 次
- 超过次数标记为 `FAILED`
- 无论成功失败都 ACK，避免死循环

### 面试加分点

可以补一句：

> 如果以后任务量更大，我会考虑把失败消息投递到死信队列，或者引入专门的 MQ 如 Kafka/RabbitMQ。

---

## 11. 数据库与 JPA

## 11.1 为什么使用 Spring Data JPA

### 面试官可能怎么问

`为什么选 JPA，不选 MyBatis？`

### 建议回答思路

这个项目业务实体比较多，典型是中后台 CRUD + 关系管理场景，JPA 的优势是：

- 开发效率高
- 实体关系映射方便
- 对简单查询很友好

### 你也可以诚实补充

对于非常复杂的 SQL 和极致性能调优场景，MyBatis 会更灵活；但这个项目以业务迭代效率为优先，因此 JPA 合适。

---

## 11.2 事务设计

### 面试官可能怎么问

`事务一般放在哪一层？为什么？`

### 建议回答思路

事务一般放 Service 层，因为 Service 负责业务编排。

### 本项目示例

[AuthService.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/modules/account/service/AuthService.java)：

```java
@Transactional(readOnly = true)
public AuthResponse login(LoginRequest request) { ... }
```

[VoiceInterviewService.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/modules/voiceinterview/service/VoiceInterviewService.java)：

```java
@Transactional
public SessionResponseDTO createSession(CreateSessionRequest request) { ... }
```

### 面试必须注意的点

根据项目规范：

- 不在事务内调用外部 API
- 不在同类内部调用 `@Transactional`
- 事务范围尽量小

这是很重要的工程经验点。

---

## 12. AI 能力设计

## 12.1 为什么引入 Spring AI

### 面试官可能怎么问

`你为什么选 Spring AI，而不是自己直接调 OpenAI 兼容接口？`

### 建议回答思路

Spring AI 的价值：

1. 屏蔽底层 HTTP 调用细节
2. 统一 Chat、Embedding、VectorStore 接口
3. 方便接入 Advisor、ToolCall、Memory 等能力
4. 便于后续切换不同兼容模型

---

## 12.2 多 Provider 注册中心设计

对应实现：

- [LlmProviderRegistry.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/common/ai/LlmProviderRegistry.java)

### 面试官可能怎么问

`你们是怎么支持多个大模型提供商的？`

### 建议回答思路

项目没有把某一个模型写死，而是抽象出 Provider Registry：

- 根据 providerId 动态创建 ChatClient
- 用缓存避免重复创建
- 支持默认模型、用户默认模型、纯文本模型、语音模型等不同形态

### 本项目示例

```java
public ChatClient getChatClient(String providerId) {
  return clientCache.computeIfAbsent(providerId, id -> createChatClient(id));
}
```

如果 provider 为空，就走默认：

```java
public ChatClient getChatClientOrDefault(String providerId) {
  if (providerId != null && !providerId.isBlank()) {
    return getChatClient(providerId);
  }
  return getDefaultChatClient();
}
```

### 这里的设计亮点

Registry 不只是创建对象，还区分了几种 client：

- 普通聊天 `getChatClient`
- 不带工具调用的结构化输出 `getPlainChatClient`
- 语音专用流式对话 `getVoiceChatClient`

这非常适合面试展开，因为它体现了“同一种模型接入能力，不同业务场景下需要不同装配策略”。

### 你可以这样总结

> 我们没有把模型调用散落在各个 Service 里，而是通过 `LlmProviderRegistry` 做统一注册、缓存和路由，这样后续新增 DashScope、DeepSeek、GLM、LM Studio 等 Provider 时，对业务代码基本无侵入。

---

## 12.3 结构化输出稳定性

对应实现：

- [StructuredOutputInvoker.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/common/ai/StructuredOutputInvoker.java)

### 面试官可能怎么问

`大模型返回 JSON 经常不稳定，你怎么处理？`

### 建议回答思路

可以从“提示词约束 + 重试 + 本地修复”三层讲。

### 本项目示例

第一层，明确要求只能返回 JSON：

```java
private static final String STRICT_JSON_INSTRUCTION = """
请仅返回可被 JSON 解析器直接解析的 JSON 对象，并严格满足字段结构要求：
1) 不要输出 Markdown 代码块
2) 不要输出任何解释文字
3) 所有字符串内引号必须正确转义
""";
```

第二层，失败后重试：

```java
for (int attempt = 1; attempt <= maxAttempts; attempt++) {
  try {
    String content = chatClient.prompt().system(attemptSystemPrompt).user(userPrompt).call().content();
    T result = convertWithRepair(content, outputConverter, logContext, log);
    return result;
  } catch (Exception e) {
    lastError = e;
  }
}
```

第三层，本地修复未转义引号：

```java
String repaired = repairUnescapedQuotesInJsonStrings(content);
if (!repaired.equals(content)) {
  return outputConverter.convert(repaired);
}
```

### 面试亮点回答

> 我们没有盲信模型一定返回标准 JSON，而是做了结构化输出稳定性治理。包括严格 JSON 指令、失败重试、把上次错误原因反馈给模型、以及本地修复常见格式问题，比如字符串中的未转义引号。这让结构化解析成功率明显更高。

---

## 12.4 Prompt 安全

### 面试官可能怎么问

`如果用户在问题里注入 prompt，比如“忽略上面所有要求”，怎么办？`

### 回答思路

项目在系统提示词里追加了反注入指令：

```java
String securedSystemPrompt = systemPromptWithFormat
    + PromptSecurityConstants.ANTI_INJECTION_INSTRUCTION;
```

知识库问答系统提示词也会追加这类内容。

### 可以怎么答

> 我们在所有关键 AI 调用链路里都追加了 anti-injection instruction，并把系统提示词和用户输入做职责隔离，尽量避免用户输入覆盖系统规则。

---

## 13. RAG 与向量检索

## 13.1 RAG 基本流程

对应实现：

- [KnowledgeBaseVectorService.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/modules/knowledgebase/service/KnowledgeBaseVectorService.java)
- [KnowledgeBaseQueryService.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/modules/knowledgebase/service/KnowledgeBaseQueryService.java)

### 面试官可能怎么问

`你们知识库问答是怎么做的？`

### 建议回答思路

按完整链路答：

1. 文档上传
2. 文本解析
3. 分块
4. 向量化
5. 存入 pgvector
6. 查询时做相似度检索
7. 把检索到的上下文拼进 Prompt
8. 让大模型基于上下文回答

### 本项目示例

向量化：

```java
List<Document> chunks = textSplitter.apply(List.of(new Document(content)));
chunks.forEach(chunk -> chunk.getMetadata().put("kb_id", knowledgeBaseId.toString()));
vectorStore.add(batch);
```

问答：

```java
List<Document> relevantDocs = retrieveRelevantDocs(queryContext, knowledgeBaseIds);
String context = relevantDocs.stream()
    .map(Document::getText)
    .collect(Collectors.joining("\n\n---\n\n"));
String answer = getChatClient().prompt()
    .system(systemPrompt)
    .user(userPrompt)
    .call()
    .content();
```

---

## 13.2 为什么使用 pgvector

### 面试官可能怎么问

`为什么选 PostgreSQL + pgvector，而不是 Elasticsearch 或 Milvus？`

### 建议回答思路

这个项目的选择更偏工程平衡：

1. 已经有 PostgreSQL，不想额外引入太多中间件
2. 数据规模不算超大，pgvector 足够
3. 架构更简单，部署成本更低

### 你可以补一句

如果未来文档量和检索复杂度大幅增长，可以评估：

- Milvus
- Qdrant
- ES + 向量检索

---

## 13.3 文本分块策略

### 面试官可能怎么问

`知识库为什么要分块？块大小怎么考虑？`

### 本项目示例

```java
this.textSplitter = TokenTextSplitter.builder().build();
```

代码注释写得很清楚：

> 每个 chunk 约 800 tokens，基于标点边界切分

### 回答要点

不分块的问题：

- 单段太长，Embedding 质量下降
- 检索粒度太粗
- 上下文窗口浪费

分块太小的问题：

- 语义不完整
- 检索结果碎片化

所以块大小要在“语义完整性”和“检索精度”之间平衡。

---

## 13.4 TopK 与相似度阈值

### 面试官可能怎么问

`RAG 检索参数你怎么调优？`

### 本项目示例

```java
SearchRequest.Builder builder = SearchRequest.builder()
    .query(query)
    .topK(Math.max(topK, 1));

if (minScore > 0) {
  builder.similarityThreshold(minScore);
}
```

### 回答要点

- `topK` 太小：召回不足
- `topK` 太大：噪声过多
- `minScore` 太高：可能查不到
- `minScore` 太低：弱相关内容太多

这个项目已经做了动态参数配置，是实际工程里比较重要的能力。

---

## 13.5 多知识库过滤

### 面试官可能怎么问

`如果用户勾选多个知识库，你怎么保证只查这些库？`

### 本项目示例

```java
builder.filterExpression(buildKbFilterExpression(knowledgeBaseIds));
```

过滤表达式：

```java
return "kb_id in ['1', '2', '3']";
```

此外还做了兜底：

如果向量库前置过滤失败，会回退为“先查，再本地按 metadata 过滤”。

```java
log.warn("向量搜索前置过滤失败，回退到本地过滤: {}", e.getMessage());
return similaritySearchFallback(query, knowledgeBaseIds, topK, minScore);
```

### 这点很适合面试讲

这体现了你对“理想路径”和“异常路径”的双重设计。

---

## 13.6 流式输出 SSE

### 面试官可能怎么问

`为什么知识库问答要做流式输出？`

### 回答思路

原因主要有三点：

1. 降低用户等待焦虑
2. 更适合长答案
3. 符合 AI 产品交互习惯

### 本项目示例

```java
Flux<String> responseFlux = promptSpec
    .user(userPrompt)
    .stream()
    .content();
```

### 你可以这样表达

> 我们在知识库问答里采用 SSE 风格的流式输出，后端通过 Reactor `Flux<String>` 持续推送模型内容，前端做打字机式渲染，整体体验比同步等待完整结果更好。

---

## 14. 语音面试设计

## 14.1 语音面试模块的核心职责

对应实现：

- [VoiceInterviewService.java](/Users/adian/demo/interview-guide/app/src/main/java/interview/guide/modules/voiceinterview/service/VoiceInterviewService.java)

### 面试官可能怎么问

`你们语音面试模块的核心设计是什么？`

### 建议回答思路

可以拆成四个关键词：

- 会话管理
- 实时对话
- 阶段流转
- 异步评估

### 本项目示例

创建会话时会保存：

- 用户信息
- 技能方向
- 难度
- JD
- 简历 ID
- 各阶段开关
- 当前阶段

```java
VoiceInterviewSessionEntity session = VoiceInterviewSessionEntity.builder()
    .userId(DEFAULT_USER_ID)
    .roleType(effectiveSkillId)
    .skillId(effectiveSkillId)
    .difficulty(...)
    .currentPhase(determineFirstPhase(request))
    .build();
```

结束会话后异步触发评估：

```java
endSession(session);
voiceEvaluateStreamProducer.sendEvaluateTask(sessionId);
```

这就是“主链路快返回，耗时评估异步化”的典型设计。

---

## 14.2 Redis 缓存会话

### 面试官可能怎么问

`为什么语音会话要放 Redis？`

### 回答要点

活跃会话是高频读写数据：

- 当前阶段
- 当前状态
- 最近消息

如果每次都查数据库：

- 延迟高
- 数据库压力大

### 本项目示例

```java
String cacheKey = getSessionCacheKey(sessionId);
RBucket<VoiceInterviewSessionEntity> bucket = redissonClient.getBucket(cacheKey);
VoiceInterviewSessionEntity cached = bucket.get();
if (cached != null) {
  return cached;
}
```

### 面试延伸

可以进一步说：

> 这里缓存的是会话热点数据，而最终持久化结果仍然会落库，所以 Redis 更像是会话态加速层，不是唯一数据源。

---

## 14.3 阶段流转设计

### 面试官可能怎么问

`你们的语音面试为什么要分阶段？`

### 回答思路

真实面试通常有结构化流程，比如：

- 自我介绍
- 技术考察
- 项目深挖
- HR 问答

项目按阶段建模后有几个好处：

1. Prompt 可以更精准
2. 时长和问题风格可配置
3. 评估维度更清晰

### 本项目示例

```java
session.setCurrentPhase(newPhase);
sessionRepository.save(session);
cacheSession(session);
```

会话结束时：

```java
session.setCurrentPhase(VoiceInterviewSessionEntity.InterviewPhase.COMPLETED);
session.setStatus(VoiceInterviewSessionStatus.COMPLETED);
```

---

## 15. 文件处理与导出

## 15.1 简历解析为什么要用 Apache Tika

### 面试官可能怎么问

`PDF、DOCX、TXT 多格式简历你怎么统一解析？`

### 建议回答思路

Apache Tika 的优势是：

- 对多种文档格式支持统一
- 降低不同文件格式解析的适配成本

### 你可以这样表述

> 项目里简历和知识库文档都存在多格式输入问题，所以选择 Tika 作为统一文本提取层，而不是为每种格式分别接一套解析器。

---

## 15.2 PDF 导出

### 面试官可能怎么问

`为什么要支持 PDF 导出？实现上会遇到什么问题？`

### 建议回答思路

导出需求常见于：

- 简历分析报告
- 面试评估报告

常见问题：

- 中文字体兼容
- 样式还原
- 分页控制
- 大文本排版

### 本项目技术选型

从 [build.gradle](/Users/adian/demo/interview-guide/app/build.gradle) 可以看到：

```gradle
implementation "com.itextpdf:itext-core:..."
implementation "com.itextpdf:font-asian:..."
implementation 'com.openhtmltopdf:openhtmltopdf-pdfbox:1.0.10'
```

这说明项目考虑了：

- 直接 PDF 渲染
- 中文字体
- HTML 转 PDF

---

## 16. 前端架构

## 16.1 为什么前端选 React + TypeScript + Vite

### 面试官可能怎么问

`前端技术栈为什么这么选？`

### 建议回答思路

- `React` 适合组件化开发
- `TypeScript` 提升类型安全
- `Vite` 启动和构建更快

### 本项目示例

[frontend/package.json](/Users/adian/demo/interview-guide/frontend/package.json) 中依赖很清晰：

- `react`
- `typescript`
- `vite`
- `axios`
- `react-router-dom`
- `framer-motion`
- `react-big-calendar`
- `recharts`

---

## 16.2 Axios 二次封装

对应实现：

- [request.ts](/Users/adian/demo/interview-guide/frontend/src/api/request.ts)

### 面试官可能怎么问

`前端为什么要封装 request 层？`

### 回答思路

封装的目的是统一处理：

- baseURL
- token 注入
- 超时
- 统一错误提示
- 文件上传特殊逻辑

### 本项目示例

请求拦截器自动带 token：

```ts
instance.interceptors.request.use((config) => {
  const token = getAuthToken();
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});
```

响应拦截器统一拆 `Result<T>`：

```ts
if (result.code === 200) {
  response.data = result.data;
  return response;
}
return Promise.reject(new Error(result.message || '请求失败'));
```

### 这段代码说明了什么

它和后端统一响应结构是配套设计的。后端统一返回 `Result<T>`，前端统一在 request 层解包，这样业务页面就能直接拿数据对象，不需要每个页面手写 `res.data.data`。

---

## 16.3 前端认证状态处理

### 面试官可能怎么问

`前端是怎么保存登录态的？`

### 本项目示例

项目把 token 保存在 Cookie：

```ts
document.cookie = `${AUTH_TOKEN_COOKIE}=${encodeURIComponent(token)}; path=/${maxAge}; SameSite=Lax`;
```

并支持解析 JWT payload 获取角色和用户 ID：

```ts
const payload = JSON.parse(decodeURIComponent(escape(window.atob(padded))));
return Array.isArray(payload.roles) ? payload.roles : [];
```

### 回答要点

可以讲清楚：

- 为什么要把 token 自动注入请求头
- 为什么前端需要知道用户角色
- 为什么页面权限控制不能只依赖前端，后端仍然要做鉴权

---

## 17. 测试设计

## 17.1 单元测试

对应示例：

- [VoiceInterviewServiceTest.java](/Users/adian/demo/interview-guide/app/src/test/java/interview/guide/modules/voiceinterview/service/VoiceInterviewServiceTest.java)

### 面试官可能怎么问

`你在这个项目里怎么做单元测试？`

### 回答思路

单元测试重点验证：

- 服务层业务逻辑
- 状态流转
- 参数边界
- 依赖交互

### 本项目示例

测试使用：

- `JUnit 5`
- `Mockito`

典型写法：

```java
@ExtendWith(MockitoExtension.class)
@Nested
@DisplayName("会话创建测试")
class CreateSessionTests { ... }
```

验证保存逻辑：

```java
verify(sessionRepository, times(1)).save(any(VoiceInterviewSessionEntity.class));
```

捕获参数：

```java
ArgumentCaptor<VoiceInterviewSessionEntity> captor =
    ArgumentCaptor.forClass(VoiceInterviewSessionEntity.class);
```

### 面试加分点

这个测试文件里还显式写了 `@Disabled` 原因，说明团队对测试债务是透明的，而不是悄悄失败。

---

## 17.2 测试分层怎么讲

### 面试官可能怎么问

`单元测试、集成测试、接口测试分别测什么？`

### 建议回答思路

- 单元测试：测 Service 逻辑和边界
- 集成测试：测 Spring 容器、数据库、Redis、JPA 查询
- 接口测试：测完整 HTTP 请求链路

### 可以结合项目说

对于这个项目：

- `AuthService`、`VoiceInterviewService` 适合做单元测试
- JPA Repository 和事务行为适合集成测试
- 登录、知识库查询、上传接口适合接口测试

---

## 18. 项目中的设计模式

## 18.1 模板方法模式

### 本项目示例

`AbstractStreamProducer` 和 `AbstractStreamConsumer`

### 面试回答

公共流程在父类中固化，可变步骤交给子类实现，典型模板方法模式。

---

## 18.2 注册表模式

### 本项目示例

`LlmProviderRegistry`

### 面试回答

统一维护和缓存 Provider，避免业务代码到处 if-else 判断模型类型。

---

## 18.3 责任链/过滤器模式

### 本项目示例

Spring Security 过滤器链，`JwtAuthenticationFilter` 是其中一环。

### 面试回答

请求进入系统后按过滤器链逐步处理认证、鉴权、异常返回，是典型责任链思想。

---

## 18.4 AOP 横切关注点

### 本项目示例

`RateLimitAspect`

### 面试回答

限流是典型横切逻辑，不属于单一业务模块，用 AOP 可以避免重复代码并保持 Controller 简洁。

---

## 19. 项目亮点怎么讲

如果面试官问：

`你觉得这个项目最有含金量的点是什么？`

可以从下面几个角度选 3 到 4 个重点展开。

### 19.1 多模型接入不是写死的

- 通过 `LlmProviderRegistry` 统一管理
- 支持默认模型和按用户配置
- 业务侧不感知底层 Provider 差异

### 19.2 结构化输出稳定性治理

- 严格 JSON Prompt
- 重试机制
- 本地修复非标准 JSON
- 指标采集

### 19.3 Redis Stream 异步解耦

- 简历分析、向量化、面试评估异步化
- 模板抽象减少重复代码
- 重试与失败状态清晰

### 19.4 RAG 设计更贴近工程实践

- 文档分块
- 向量检索
- TopK 与阈值调优
- 多知识库过滤
- 流式返回

### 19.5 前后端协议统一

- 后端统一 `Result<T>`
- 前端统一 request 拦截器
- 错误处理一致

---

## 20. 常见高频面试题与参考回答

## 20.1 你在这个项目里解决过什么技术难点？

参考回答：

> 一个比较典型的难点是大模型结构化输出不稳定。我们需要模型返回标准 JSON，但实际经常会带 Markdown 代码块、解释文字，或者 JSON 字符串中的引号没转义。为了解决这个问题，我在项目里封装了 `StructuredOutputInvoker`，通过严格 JSON 指令、失败重试、把上次解析错误反馈给模型，以及本地修复常见格式问题，提升了解析成功率。这比简单调用模型后直接 `parse` 更稳定，也更适合生产场景。

## 20.2 你们为什么要用 Redis Stream？

参考回答：

> 因为简历分析、知识库向量化、面试评估都是典型耗时任务，不适合同步阻塞接口。相比直接开线程池，Redis Stream 更适合做跨实例消费、失败重试和状态解耦。我们还把公共逻辑抽成了 `AbstractStreamProducer/Consumer`，让不同任务复用一套异步处理骨架。

## 20.3 你们的 RAG 为什么不是简单把问题扔给模型？

参考回答：

> 因为模型本身不一定知道企业私有知识或者最新文档。我们会先把文档切块并向量化存入 pgvector，查询时先召回相关片段，再把这些片段作为上下文拼到 Prompt 中，这样答案更可控，也更能减少幻觉。项目里还支持多知识库筛选、相似度阈值和流式输出。

## 20.4 为什么要统一返回 HTTP 200？

参考回答：

> 这是一个偏前后端一体化的中后台项目，我们希望前端只认统一的 `Result<T>` 协议，用业务码区分错误，这样接口层和页面层处理会比较简单。当然如果是对外开放 API，我会更倾向同时遵循标准 HTTP 状态码语义。

## 20.5 这个项目如果让你继续优化，你会做什么？

参考回答：

可以从下面几项里选：

- 把语音链路从服务端音频中转升级到 WebRTC，降低延迟
- 给异步任务增加死信队列和更完整的监控告警
- 为 RAG 引入更细粒度的 chunk overlap 与 rerank
- 对前端增加更完善的权限路由守卫和埋点
- 对 AI 调用增加熔断、限流和降级策略

---

## 21. 适合背诵的项目介绍模板

## 21.1 1 分钟版本

> 我做的是一个智能 AI 面试平台，后端基于 Spring Boot 4、Java 21、Spring AI，前端基于 React 18 和 TypeScript。核心功能包括简历解析和评分、文字和语音模拟面试、RAG 知识库问答、面试日程管理以及多模型 Provider 配置。技术上我重点做了 JWT 认证、统一异常和响应封装、Redis Stream 异步任务、pgvector 向量检索、多模型路由和结构化输出稳定性治理。

## 21.2 3 分钟版本

> 这个项目的定位是一个面向求职者和培训场景的智能面试平台。后端采用 Spring Boot 4 + Java 21，使用 Spring AI 对接多个 OpenAI 兼容模型，数据库是 PostgreSQL + pgvector，Redis 主要用于限流、会话缓存和异步任务。  
> 在架构上，我们按业务模块拆分，比如账号、简历、知识库、文字面试、语音面试、面试日程等，每个模块内部再分 Controller、Service、Repository。  
> 其中我觉得比较有技术含量的几个点是：第一，多模型接入没有写死，而是通过 `LlmProviderRegistry` 做统一注册、缓存和路由；第二，模型结构化输出不稳定，我封装了 `StructuredOutputInvoker`，做了重试和 JSON 修复；第三，简历分析、知识库向量化、面试评估都通过 Redis Stream 异步化，并抽象了 Producer/Consumer 模板；第四，RAG 模块支持文档分块、向量检索、多知识库过滤和流式输出。整体上这个项目既覆盖了典型中后台能力，也包含了 AI 工程落地的核心问题。

---

## 22. 学习建议

如果你要拿这份文档复习，建议分四轮：

### 第一轮：讲清项目

目标：

- 不看文档，能讲 1 分钟项目介绍
- 知道每个模块做什么

### 第二轮：讲清核心技术

重点背这几块：

- JWT + Spring Security
- Redis Stream
- RAG
- 多 LLM Provider
- 结构化输出

### 第三轮：讲清设计原因

重点回答“为什么这样做”：

- 为什么要异步
- 为什么要统一响应
- 为什么要用 pgvector
- 为什么要做多模型路由

### 第四轮：准备追问

每个知识点都多想两个问题：

1. 如果流量变大怎么办？
2. 如果功能更复杂怎么办？

这样你的回答会更像“有工程经验的人”，而不是只会背概念。

---

## 23. 结语

这份文档的核心目标不是帮你死记硬背，而是帮你建立一套“基于真实项目讲技术”的表达方式。

如果你面试时能做到下面这三点，效果通常会很好：

1. 先讲业务背景，再讲技术方案。
2. 不只说“怎么做”，还说“为什么这样做”。
3. 不只说优点，也能说方案的边界和后续优化方向。

这样面试官会更容易判断你是真的做过，而不是只看过代码。
