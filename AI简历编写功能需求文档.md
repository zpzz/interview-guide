# AI 简历编写功能需求文档

## 1. 背景

当前系统已经具备以下基础能力：

- 用户登录、JWT 鉴权、普通用户 / 管理员权限隔离。
- 简历上传、文件解析、简历文本提取、简历分析。
- 多 Provider 大模型配置与账号级默认模型设置。
- 文件存储、模板资源管理、HTML 预览、浏览器打印导出。

在此基础上，系统新增了“AI 简历编写”模块，用于基于**已上传简历**进行简历优化、排版和导出，而不是重新手工录入完整简历信息。该模块已经内置 `resume-master` 的模板资源和提示词思路，支持通过大模型直接生成完整 HTML 简历。

本需求文档的目标，是将当前页面和后端实现沉淀为一份清晰、可维护、与现状一致的产品说明和数据库设计说明。

---

## 2. 功能目标

### 2.1 核心目标

- 在左侧菜单提供“AI 简历编写”入口。
- 用户基于**已有上传简历**生成优化后的 HTML 简历。
- 支持输入 JD、其他描述、目标页数、可选模型等参数，引导大模型生成更符合预期的结果。
- 支持选择内置模板或上传模板 PDF 作为排版参考。
- 支持在线预览生成后的 HTML 简历。
- 支持导出 HTML。
- 支持通过浏览器打印导出 PDF。
- 支持查看历史生成记录，并可回看和删除。
- 所有数据按用户隔离；管理员可查看全部记录，普通用户只能查看自己的记录和模板资源。

### 2.2 非目标

本期不包含以下能力：

- 从零手工录入完整简历表单并生成。
- 富文本 / 拖拽式简历编辑器。
- 后端统一生成高保真 PDF。
- Word 导出。
- 多版本 diff 对比。
- 简历块级可视化拖拽排版。

---

## 3. 页面功能说明

## 3.1 页面入口

- 左侧菜单：`AI 简历编写`
- 页面路径：前端独立页面 `ResumeMakerPage`

## 3.2 页面整体布局

页面采用左右双栏布局：

- 左侧：配置与历史记录区域
- 右侧：在线预览区域

顶部操作区包含：

- `生成简历`
- `导出 HTML`
- `导出 PDF`

其中：

- `导出 PDF` 当前通过浏览器打印能力实现，不走后端排版引擎。
- 右侧预览区域展示模型生成后的完整 HTML。

## 3.3 左侧页签

左侧包含两个页签：

### 3.3.1 生成配置

用于发起新的简历生成请求，页面字段如下：

| 字段 | 必填 | 说明 |
|------|------|------|
| 简历标题 | 否 | 生成记录标题；为空时后端根据原简历名和目标岗位自动生成 |
| 目标岗位 | 否 | 用于指导文案优化方向 |
| 目标页数 | 是 | 期望生成后的页数，范围 1-10，默认 1 |
| 选择已上传简历 | 是 | 从用户历史上传简历中选择一份作为原始内容来源 |
| 简历模板 PDF | 否 | 可选择内置模板或用户上传的模板 PDF 作为版式参考 |
| 指定模型 | 否 | 指定本次生成使用的文字模型；为空则使用当前账号默认模型 |
| JD 文本 | 否 | 作为定向优化依据 |
| 其他描述 | 否 | 用于补充风格、重点、表达约束、内容偏好等 |

辅助功能：

- `去上传简历`：跳转到简历上传页
- `查看简历详情`：跳转到当前选中简历的详情页
- `上传模板 PDF`：上传用户自定义模板资源
- `内置模板参考`：查看系统内置模板缩略图、参考 PDF、参考 HTML

### 3.3.2 已生成简历

用于展示历史生成记录，列表项展示：

- 标题
- 来源简历名称
- 目标岗位（如有）
- 目标页数（如有）
- 更新时间

支持操作：

- 查看：将该条历史记录加载到右侧预览，并同步左侧配置
- 删除：删除当前生成记录，带二次确认

## 3.4 右侧在线预览

右侧为 HTML 预览区域，显示模型生成后的简历内容。

预览区状态：

- 未生成时：展示空态提示
- 已生成时：通过 `iframe + srcDoc` 渲染 HTML

页面当前额外处理逻辑：

- 点击“生成简历”时，会清空旧预览，再加载新结果
- 点击历史记录中的“查看”时，会在右侧展示历史 HTML
- 打印前会注入打印样式，修正分页、容器高度和末页补齐问题

---

## 4. 业务流程

## 4.1 简历生成流程

```text
用户选择已上传简历
-> 系统读取解析后的简历文本
-> 用户可选填写目标岗位 / JD / 其他描述 / 目标页数 / 指定模型
-> 用户可选选择内置模板或上传模板 PDF
-> 后端读取模板参考 HTML
-> 后端组装 Prompt
-> 大模型直接输出完整 HTML
-> 前端展示在线预览
-> 后端保存生成记录
```

## 4.2 模板处理流程

### 4.2.1 内置模板

系统内置多套模板资源，包含：

- 模板元数据
- 参考 HTML
- 参考 PDF
- 缩略图

生成时：

- 前端只传模板 ID
- 后端根据模板 ID 读取对应参考 HTML
- 将参考 HTML 作为 Prompt 输入给模型

### 4.2.2 用户上传模板

用户可上传自己的模板 PDF。

生成时：

- 后端读取模板文件
- 将模板内容转换为可供模型参考的 HTML / 文本结构
- 一并输入给大模型

说明：

- 当前模板 PDF 主要是“参考模板”，不是后端强制套版。
- 最终排版仍由大模型生成完整 HTML。

## 4.3 导出流程

### 4.3.1 导出 HTML

- 直接将当前预览 HTML 作为文件下载

### 4.3.2 导出 PDF

- 当前不走后端 PDF 排版服务
- 前端通过预览 iframe 调用浏览器打印
- 导出前注入打印样式，包含：
  - A4 页面尺寸与边距
  - 分页控制
  - 末页补满
  - 双栏模板高度补齐

---

## 5. 后端实现说明

## 5.1 模块目录

当前后端模块目录：

```text
app/src/main/java/interview/guide/modules/resumemaker/
```

已包含核心能力：

- `ResumeMakerController`
- `ResumeMakerService`
- `ResumeMakerAiService`
- `ResumeMakerTemplateService`
- `ResumeTemplateAssetService`
- `ResumeMakerPdfService`
- DTO / Entity / Repository

## 5.2 后端接口设计

### 5.2.1 模板接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/resume-maker/templates/builtin` | 查询内置模板列表 |
| GET | `/api/resume-maker/template-assets` | 查询当前用户上传模板列表 |
| POST | `/api/resume-maker/template-assets/upload` | 上传模板 PDF |
| DELETE | `/api/resume-maker/template-assets/{id}` | 删除模板 PDF |

### 5.2.2 简历生成接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/resume-maker/preview/generate` | 生成 HTML 简历 |
| POST | `/api/resume-maker/preview/optimize-by-jd` | 预留优化接口，当前页面主流程使用 generate |

### 5.2.3 导出接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/resume-maker/preview/export-pdf` | 后端 PDF 导出接口，当前页面未作为主导出链路使用 |

### 5.2.4 生成记录接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/resume-maker/generated-resumes` | 查询生成记录列表 |
| GET | `/api/resume-maker/generated-resumes/{id}` | 查询生成记录详情 |
| DELETE | `/api/resume-maker/generated-resumes/{id}` | 删除生成记录 |

## 5.3 提示词设计

提示词目录：

```text
app/src/main/resources/prompts/resume-maker/
```

当前已使用的关键 Prompt：

- `resume-maker-html-system.st`
- `resume-maker-html-user.st`

当前设计特征：

- 模型直接输出完整 HTML
- 输入包含：
  - 简历标题
  - 目标岗位
  - JD 文本
  - 其他描述
  - 目标页数
  - 简历原文
  - 模板参考 HTML

当前 Prompt 已明确约束：

- 只返回完整 HTML
- 适配 A4 打印
- 页数尽量满足目标页数
- 页数必须为整数页
- 不得虚构经历和身份字段
- 可根据模板参考 HTML 调整布局风格

## 5.4 模型调用策略

当前实现采用：

```text
后端组装完整 Prompt
-> 调用 ChatClient
-> 模型直接输出 HTML
-> 后端做基础合法性检查
-> 保存并返回 HTML
```

说明：

- 当前没有采用“结构化 JSON -> 模板渲染 HTML”的两段式方案。
- 当前是“模型直接负责内容优化 + 页面排版”。
- 后端只负责：
  - 提供原始文本
  - 提供模板参考
  - 拼装 Prompt
  - 结果校验
  - 数据存储

---

## 6. 前端实现说明

## 6.1 页面状态

前端当前维护的主要状态包括：

- 当前页签
- Provider 列表
- 已上传简历列表
- 内置模板列表
- 用户上传模板列表
- 生成记录列表
- 当前选中简历
- 当前选中模板
- 当前预览 HTML
- 当前生成参数（标题、目标岗位、目标页数、JD、其他描述、指定模型）

## 6.2 关键前端交互

- 页面初始化时自动加载：
  - Provider 列表
  - 已上传简历
  - 用户模板
  - 内置模板
  - 生成记录
- 若存在已上传简历，默认自动选中第一份并读取解析文本
- 生成成功后自动刷新生成记录列表
- 删除生成记录后，若当前右侧预览正展示该记录，则同步清空

## 6.3 打印增强

当前前端对打印做了额外处理：

- 打印前注入 `@page` 和分页控制样式
- 移除模板中不利于打印的固定高度 / overflow 设置
- 自动扩展简历根容器高度，使导出结果尽量补满整页

---

## 7. 数据库设计

## 7.1 生成记录表 `generated_resumes`

用于保存每次生成后的简历结果。

### 字段设计

| 字段名 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| user_id | bigint | 所属用户 ID |
| source_resume_id | bigint | 来源简历 ID |
| source_resume_name | varchar(255) | 来源简历名称 |
| title | varchar(255) | 当前生成记录标题 |
| target_role | varchar(255) | 目标岗位 |
| jd_text | text | JD 文本 |
| builtin_template_id | varchar(64) | 内置模板 ID |
| template_asset_id | bigint | 用户上传模板资源 ID |
| provider_id | varchar(64) | 本次指定模型 ID |
| target_page_count | int | 目标页数 |
| html_content | text | 生成后的完整 HTML |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

### 索引

| 索引名 | 字段 | 说明 |
|------|------|------|
| idx_generated_resume_user_updated | user_id, updated_at | 按用户查询历史记录并按更新时间排序 |

### 说明

- 一条记录对应一次成功生成的 HTML 简历
- 不保存“草稿表”
- 当前没有单独存储结构化 JSON 内容
- 历史查看直接读取 `html_content`

## 7.2 模板资源表 `resume_template_assets`

用于保存用户上传的模板 PDF 资源。

### 字段设计

| 字段名 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| user_id | bigint | 上传用户 ID |
| original_filename | varchar(255) | 原始文件名 |
| content_type | varchar(128) | 文件 MIME 类型 |
| file_size | bigint | 文件大小 |
| storage_key | varchar(500) | 文件存储 key |
| storage_url | varchar(1000) | 文件访问地址 |
| uploaded_at | datetime | 上传时间 |

### 索引

| 索引名 | 字段 | 说明 |
|------|------|------|
| idx_resume_template_asset_user_uploaded | userId, uploadedAt | 按用户查看模板列表 |

### 说明

- 当前仅保存用户上传模板
- 内置模板不落数据库，直接放在后端资源目录
- 上传模板在生成时作为参考资源输入给模型

---

## 8. 内置模板资源设计

内置模板资源位于后端静态资源目录，包含：

- 参考 HTML
- 参考 PDF
- 缩略图
- 模板元数据 JSON

当前设计目标：

- 前端通过接口查询模板列表
- 实际模板文件统一放在后端，避免前后端分散
- 支持“查看 PDF / 查看 HTML / 选用模板”

---

## 9. 权限与数据隔离

## 9.1 普通用户

- 只能查看自己的上传模板
- 只能查看自己的生成记录
- 只能删除自己的模板和生成记录
- 可使用自己的默认模型或显式指定自己可见的模型

## 9.2 管理员

- 可查看所有生成记录
- 可查看所有用户生成结果
- 模板资源和简历生成记录在后端服务层按当前账号进行可见性控制

---

## 10. 当前实现与后续优化建议

## 10.1 当前已实现

- AI 简历编写页面
- 已上传简历驱动的生成模式
- JD / 其他描述 / 指定模型 / 目标页数输入
- 内置模板与上传模板能力
- HTML 在线预览
- HTML 导出
- 浏览器打印导出 PDF
- 生成记录回看与删除
- 账号级数据隔离

## 10.2 建议后续优化

- 增加“生成后自动检测页数并二次微调”的闭环
- 增加“导出前页数与留白检测”
- 支持更稳定的后端高保真 PDF 导出
- 增加模板能力校验（例如哪些模型更适合复杂排版）
- 增加简历版本对比与复制生成功能
- 增加历史记录搜索和筛选

---

## 11. 总结

当前“AI 简历编写”模块已经从最初的“从零录入信息生成简历”的设想，演进为一个更贴近真实场景的产品形态：

- 以**已上传简历**为输入源
- 通过大模型直接生成完整 HTML 简历
- 支持模板参考、页数控制、在线预览和导出
- 支持历史记录管理
- 数据按用户隔离

因此，后续所有开发和文档应以当前这条真实链路为准，不再沿用旧版“草稿表单 + 结构化 JSON + 后端模板渲染”的描述。
