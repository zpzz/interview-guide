请基于以下内容生成完整 HTML 简历：

简历标题：
<title>

目标岗位：
<targetRole>

目标页数：
<targetPageCount> 页

目标 JD：
<jdText>

其他描述：
<extraNotes>

简历原文：
<resumeText>

模板参考：
<templateReferenceHtml>

补充要求：
1. 若提供模板参考，优先参考其版式结构、模块顺序、颜色风格和分栏方式，但不要复用模板示例文案。
2. 若提供模板参考，必须沿用模板主色，不允许自行更换主色系；标题、分隔条、标签、强调色应与模板主色保持一致或属于同一色系。
3. 你需要自己完成内容整理、模块划分、文案优化和 HTML 排版。
4. 最终只返回完整 HTML。
5. 不需要在 HTML 中精确控制分页，也不需要输出固定数量的 `.page`。
6. 不要为了凑页数强行裁剪、隐藏或截断正文内容，正文应按正常文档流自然展开。
7. 前端会在导出 PDF 时负责分页，因此你只需要输出完整、自然、稳定的 HTML 内容即可。
