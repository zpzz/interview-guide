请基于以下信息，对简历内容做定向优化，使其更匹配岗位“<targetRole>”。

用户原始资料（可能包含原始个人信息文本）：
<profileJson>

目标 JD：
<jdText>

优化要求：
1. 重新组织亮点顺序，突出 JD 中的关键能力。
2. 对项目和工作经历做更强的岗位匹配表达。
3. 删除明显无关的冗余描述，但不要伪造新经历。
4. 必须以用户原始文本中真实存在的信息为依据，不要虚构新事实。
5. 不要返回 HTML，不要返回 Markdown，只返回结构化 JSON。
