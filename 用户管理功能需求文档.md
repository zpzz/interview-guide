# 用户管理功能需求文档

## 1. 背景

当前系统已经支持账号登录、角色权限和用户数据隔离，但还缺少管理员维护用户账号的后台能力。为方便管理员管理系统账号，需要新增“用户管理”模块，仅管理员可访问，用于对用户信息进行增删改查和角色维护。

## 2. 目标

- 左侧菜单新增“用户管理”功能入口。
- 仅管理员可以访问用户管理页面和相关接口。
- 管理员可以查看用户列表。
- 管理员可以新增普通用户或管理员用户。
- 管理员可以编辑用户基础信息。
- 管理员可以启用或禁用用户。
- 管理员可以重置用户密码。
- 管理员可以删除用户。
- 默认 `admin` 账号不可修改、不可禁用、不可重置密码、不可删除。
- 普通用户不能看到用户管理菜单，也不能访问用户管理接口。

## 3. 权限规则

| 操作 | 管理员 | 普通用户 |
|------|--------|----------|
| 查看用户管理菜单 | 允许 | 禁止 |
| 查看用户列表 | 允许 | 禁止 |
| 新增用户 | 允许 | 禁止 |
| 编辑用户 | 允许 | 禁止 |
| 启用/禁用用户 | 允许 | 禁止 |
| 重置密码 | 允许 | 禁止 |
| 删除用户 | 允许 | 禁止 |

特殊规则：

- 默认 `admin` 账号是系统内置超级管理员账号。
- `admin` 账号不能被编辑。
- `admin` 账号不能被禁用。
- `admin` 账号不能被删除。
- `admin` 账号不能被重置密码，本期不提供任何页面或接口修改默认 `admin` 密码。
- 用户不能删除自己，避免当前管理员误删自身导致无法继续管理系统。
- 当前管理员不能移除自己的管理员角色。
- 当前管理员不能禁用自己。
- 禁用用户后，该用户不能登录系统。
- 已登录但后续被禁用的用户，下一次登录时必须被拒绝。

## 4. 后端需求

### 4.1 用户管理接口

新增管理员用户管理接口前缀：

```text
/api/admin/users
```

所有接口必须校验当前登录用户是否为 `ADMIN` 角色。

普通用户调用这些接口时，返回无权限错误。

### 4.2 用户列表

```text
GET /api/admin/users
```

支持查询用户列表。

建议支持查询参数：

| 参数 | 说明 |
|------|------|
| keyword | 按账号或昵称模糊搜索 |
| role | 按角色过滤，例如 USER、ADMIN |
| enabled | 按启用状态过滤 |
| page | 页码 |
| size | 每页数量 |

响应字段：

| 字段 | 说明 |
|------|------|
| id | 用户 ID |
| username | 登录账号 |
| nickname | 用户昵称 |
| enabled | 是否启用 |
| roles | 角色列表 |
| createdAt | 创建时间 |
| updatedAt | 更新时间 |
| protectedAccount | 是否内置保护账号 |

### 4.3 用户详情

```text
GET /api/admin/users/{id}
```

返回单个用户详情。

### 4.4 新增用户

```text
POST /api/admin/users
```

请求参数：

```json
{
  "username": "user001",
  "password": "123456",
  "nickname": "用户001",
  "enabled": true,
  "roles": ["USER"]
}
```

规则：

- `username` 必填且唯一。
- `password` 必填，必须加密保存。
- `roles` 固定提交一个角色。
- 角色只能从系统已存在角色中选择。
- 禁止新增用户名为 `admin` 的用户。

### 4.5 编辑用户

```text
PUT /api/admin/users/{id}
```

请求参数：

```json
{
  "nickname": "新昵称",
  "enabled": true,
  "roles": ["USER"]
}
```

规则：

- 不允许修改 `username`。
- 不允许修改默认 `admin` 账号。
- 不允许禁用默认 `admin` 账号。
- 角色只能选择一个。
- 不能把当前登录管理员自己的角色改成非管理员。
- 不能禁用当前登录管理员自己。

### 4.6 重置密码

```text
PUT /api/admin/users/{id}/password
```

请求参数：

```json
{
  "password": "newPassword123"
}
```

规则：

- 密码必须加密保存。
- 不允许重置默认 `admin` 账号密码。
- 密码不能为空。

### 4.7 删除用户

```text
DELETE /api/admin/users/{id}
```

规则：

- 不允许删除默认 `admin` 账号。
- 不允许删除当前登录用户自己。
- 删除用户前需要考虑业务数据处理策略。

建议采用保留业务数据策略：

- 本期删除用户时，只删除账号及用户角色关系。
- 不删除该用户创建的简历、面试记录、Provider 等业务数据。
- 被删除用户的数据仍保留在数据库中。
- 管理员仍可通过全局视角看到历史业务数据。
- 后续可扩展为软删除字段 `deleted_at`。

已确认规则：

- 默认 `admin` 密码不能重置。
- 删除用户时，本期先不删除该用户的业务数据。

## 5. 数据库设计

复用现有表：

- `users`
- `roles`
- `user_roles`

本期建议暂不新增表。

如后续需要支持软删除，可在 `users` 表增加：

| 字段 | 说明 |
|------|------|
| deleted | 是否删除 |
| deleted_at | 删除时间 |

## 6. 前端需求

### 6.1 左侧菜单

在左侧菜单新增“用户管理”菜单项。

建议位置：

```text
系统
- 设置
- 用户管理
```

显示规则：

- 仅管理员可见。
- 普通用户不展示该菜单。

### 6.2 用户管理页面

页面路径建议：

```text
/admin/users
```

页面功能：

- 用户列表表格。
- 搜索框。
- 角色筛选。
- 启用状态筛选。
- 新增用户按钮。
- 编辑按钮。
- 重置密码按钮。
- 删除按钮。

用户列表字段：

| 字段 | 说明 |
|------|------|
| 账号 | username |
| 昵称 | nickname |
| 角色 | roles |
| 状态 | enabled |
| 创建时间 | createdAt |
| 更新时间 | updatedAt |
| 操作 | 编辑、重置密码、删除 |

### 6.3 新增用户弹窗

表单字段：

- 账号。
- 初始密码。
- 昵称。
- 启用状态。
- 角色。

校验规则：

- 账号必填。
- 密码必填。
- 只能选择一个角色。

### 6.4 编辑用户弹窗

表单字段：

- 账号，只读。
- 昵称。
- 启用状态。
- 角色。

限制：

- 默认 `admin` 账号不可编辑。
- 当前管理员不能把自己的管理员角色移除。
- 当前管理员不能禁用自己。

### 6.5 重置密码弹窗

表单字段：

- 新密码。
- 确认新密码。

规则：

- 两次密码必须一致。
- 默认 `admin` 账号不可重置密码。

### 6.6 删除确认

点击删除时弹出确认框。

提示内容：

```text
确定要删除用户 “{username}” 吗？删除后该账号将无法登录，但历史业务数据会保留。
```

限制：

- 默认 `admin` 账号不可删除。
- 当前管理员不能删除自己。

## 7. 验收标准

- 普通用户看不到“用户管理”菜单。
- 普通用户访问 `/admin/users` 会被重定向或提示无权限。
- 普通用户调用 `/api/admin/users/**` 接口返回无权限。
- 管理员可以查看用户列表。
- 管理员可以新增用户。
- 管理员可以编辑非保护用户的信息和角色。
- 管理员可以禁用非保护用户。
- 被禁用用户不能登录。
- 管理员可以重置非保护用户密码。
- 管理员可以删除非保护用户。
- 默认 `admin` 账号不能修改、不能禁用、不能重置密码、不能删除。
- 当前登录管理员不能删除自己。
- 当前登录管理员不能移除自己的管理员角色。
- 当前登录管理员不能禁用自己。

## 8. 后续扩展

- 用户软删除。
- 用户操作日志审计。
- 登录日志。
- 用户详情页展示该用户创建的简历、面试记录、Provider。
- 批量启用/禁用用户。
- 用户注册审批。
- 更细粒度的菜单权限和按钮权限。
