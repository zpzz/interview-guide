# AI Interview Platform 云服务器部署操作文档

## 1. 部署方式

本项目推荐直接把项目代码放到云服务器上，然后使用项目根目录已有的 `docker-compose.yml` 一键构建和启动。

核心命令：

```bash
docker compose up -d --build
```

这个命令会自动完成：

- 构建后端 Spring Boot 镜像。
- 构建前端 React + Nginx 镜像。
- 启动 PostgreSQL + pgvector。
- 启动 Redis。
- 启动 MinIO。
- 自动创建对象存储 bucket。
- 启动前端和后端服务。

## 2. 服务说明

| 服务 | 容器名 | 说明 | 端口 |
|------|--------|------|------|
| frontend | interview-frontend | 前端页面 + Nginx 反向代理 | 80 |
| app | interview-app | Spring Boot 后端 | 8080 |
| postgres | interview-postgres | PostgreSQL + pgvector | 5432 |
| redis | interview-redis | Redis 缓存和异步队列 | 6379 |
| minio | interview-minio | S3 兼容对象存储 | 9000 / 9001 |

访问入口：

```text
http://服务器IP
```

生产环境建议只对公网开放：

```text
22 SSH
80 HTTP
443 HTTPS
```

不要把 `5432`、`6379`、`8080`、`9000`、`9001` 直接开放到公网。

## 3. 服务器准备

建议服务器配置：

- CPU：2 核及以上
- 内存：4 GB 及以上
- 磁盘：40 GB 及以上
- 系统：Ubuntu 22.04 / Ubuntu 24.04 / Debian 12

登录服务器：

```bash
ssh root@你的服务器IP
```

更新系统：

```bash
apt update
apt upgrade -y
```

安装基础工具：

```bash
apt install -y git curl vim ca-certificates gnupg ufw
```

## 4. 安装 Docker

安装 Docker：

```bash
curl -fsSL https://get.docker.com | bash
```

启动 Docker：

```bash
systemctl enable docker
systemctl start docker
```

检查安装结果：

```bash
docker version
docker compose version
```

## 5. 上传或拉取项目代码

方式一：服务器直接拉取 Git 仓库。

```bash
mkdir -p /opt/apps
cd /opt/apps
git clone <你的项目仓库地址> interview-guide
cd interview-guide
```

方式二：本地打包上传代码。

本地执行：

```bash
cd /Users/adian/demo
tar --exclude='interview-guide/.git' \
  --exclude='interview-guide/frontend/node_modules' \
  --exclude='interview-guide/app/build' \
  --exclude='interview-guide/frontend/dist' \
  -czf interview-guide.tar.gz interview-guide

scp interview-guide.tar.gz root@你的服务器IP:/opt/apps/
```

服务器执行：

```bash
cd /opt/apps
tar -xzf interview-guide.tar.gz
cd interview-guide
```

## 6. 配置 `.env`

项目根目录已经有 `.env.example`，先复制一份：

```bash
cp .env.example .env
```

编辑 `.env`：

```bash
vim .env
```

生产环境建议至少配置：

```env
# 数据库
POSTGRES_PASSWORD=请改成强密码

# AI 模型
AI_BAILIAN_API_KEY=你的阿里云百炼DashScopeKey
AI_MODEL=qwen3.5-flash

# 可选 Provider
PROVIDER_KIMI_API_KEY=
PROVIDER_KIMI_MODEL=kimi-latest
PROVIDER_DEEPSEEK_API_KEY=
PROVIDER_DEEPSEEK_MODEL=deepseek-v4-flash
PROVIDER_GLM_API_KEY=
PROVIDER_GLM_MODEL=glm-5

# 面试参数
APP_INTERVIEW_FOLLOW_UP_COUNT=1
APP_INTERVIEW_EVALUATION_BATCH_SIZE=8
```

注意：

- `.env` 不要提交到 Git。
- `POSTGRES_PASSWORD` 会被 `docker-compose.yml` 读取，用于 PostgreSQL 和后端连接。
- `AI_BAILIAN_API_KEY` 必须配置，否则简历分析、面试、语音等 AI 能力会失败。

## 7. 生产安全配置建议

当前项目新增了 JWT 登录认证，正式部署建议不要使用默认 JWT 密钥。

生成随机密钥：

```bash
openssl rand -base64 32
```

建议在 `docker-compose.yml` 的 `app.environment` 下增加：

```yaml
      APP_AUTH_JWT_SECRET: ${APP_AUTH_JWT_SECRET}
      APP_AUTH_TOKEN_TTL_SECONDS: ${APP_AUTH_TOKEN_TTL_SECONDS:-604800}
      APP_AI_CONFIG_ENCRYPTION_KEY: ${APP_AI_CONFIG_ENCRYPTION_KEY}
      APP_AI_CONFIG_REQUIRE_ENCRYPTION_KEY: ${APP_AI_CONFIG_REQUIRE_ENCRYPTION_KEY:-true}
```

然后在 `.env` 中增加：

```env
APP_AUTH_JWT_SECRET=上一步生成的随机密钥
APP_AUTH_TOKEN_TTL_SECONDS=604800
APP_AI_CONFIG_ENCRYPTION_KEY=另一个随机密钥
APP_AI_CONFIG_REQUIRE_ENCRYPTION_KEY=true
```

如果不加这些配置，系统仍然可以启动，但不建议用于正式生产环境。

## 8. 启动项目

在项目根目录执行：

```bash
docker compose up -d --build
```

查看容器状态：

```bash
docker compose ps
```

查看全部日志：

```bash
docker compose logs -f
```

查看后端日志：

```bash
docker logs -f interview-app
```

查看前端日志：

```bash
docker logs -f interview-frontend
```

启动成功后访问：

```text
http://你的服务器IP
```

默认账号：

```text
管理员：admin / admin123
普通用户：user / user123
```

上线后建议尽快修改默认密码。

## 9. 防火墙和安全组

服务器防火墙：

```bash
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable
ufw status
```

云厂商安全组也只建议开放：

```text
22
80
443
```

如果需要临时访问 MinIO 控制台 `9001`，建议使用 SSH 隧道，不要长期开放公网端口。

本地执行：

```bash
ssh -L 9001:127.0.0.1:9001 root@你的服务器IP
```

然后本地浏览器访问：

```text
http://localhost:9001
```

## 10. 域名和 HTTPS

如果有域名，先在域名 DNS 控制台添加 A 记录：

```text
记录类型：A
记录值：你的服务器公网 IP
```

当前前端容器已经监听服务器 `80` 端口，可以直接通过：

```text
http://你的域名
```

访问系统。

如果要配置 HTTPS，推荐使用云厂商负载均衡或在宿主机安装 Nginx。使用宿主机 Nginx 时，需要避免和前端容器同时占用 `80`。

一种做法是把 `docker-compose.yml` 中前端端口：

```yaml
ports:
  - "80:80"
```

改成：

```yaml
ports:
  - "127.0.0.1:8081:80"
```

然后宿主机 Nginx 反向代理到：

```text
http://127.0.0.1:8081
```

Nginx 示例：

```nginx
server {
  listen 80;
  server_name 你的域名;

  location / {
    proxy_pass http://127.0.0.1:8081;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}
```

申请证书：

```bash
apt install -y nginx certbot python3-certbot-nginx
certbot --nginx -d 你的域名
```

## 11. 更新发布

进入项目目录：

```bash
cd /opt/apps/interview-guide
```

如果是 Git 部署：

```bash
git pull
docker compose up -d --build
```

如果是本地上传代码包：

```bash
cd /opt/apps
tar -xzf interview-guide.tar.gz
cd interview-guide
docker compose up -d --build
```

清理旧镜像：

```bash
docker image prune -f
```

## 12. 常用运维命令

查看服务：

```bash
docker compose ps
```

重启后端：

```bash
docker compose restart app
```

重启前端：

```bash
docker compose restart frontend
```

重启全部服务：

```bash
docker compose restart
```

停止服务：

```bash
docker compose down
```

停止并删除数据：

```bash
docker compose down -v
```

注意：`docker compose down -v` 会删除 PostgreSQL、Redis、MinIO 的数据卷，生产环境不要随便执行。

## 13. 数据备份

备份 PostgreSQL：

```bash
mkdir -p /opt/backups/interview-guide
docker exec interview-postgres pg_dump -U postgres interview_guide \
  > /opt/backups/interview-guide/interview_guide_$(date +%F_%H%M%S).sql
```

恢复 PostgreSQL：

```bash
cat /opt/backups/interview-guide/你的备份.sql \
  | docker exec -i interview-postgres psql -U postgres interview_guide
```

查看数据卷：

```bash
docker volume ls | grep interview
```

## 14. 上线检查清单

- `.env` 已配置真实 `AI_BAILIAN_API_KEY`。
- `POSTGRES_PASSWORD` 已改成强密码。
- 已配置生产用 `APP_AUTH_JWT_SECRET`。
- 云服务器安全组只开放 `22`、`80`、`443`。
- `docker compose ps` 中服务都是 running 或 healthy。
- 浏览器可以打开前端页面。
- 可以使用管理员账号登录。
- 管理员已在系统设置中配置可用模型。
- 已测试简历上传和简历分析。
- 已测试模拟面试。
- 已测试知识库上传和 RAG 问答。

## 15. 常见问题

### 15.1 `docker compose up -d --build` 很慢

第一次构建会下载 Gradle、Node、Java、Nginx、PostgreSQL、Redis、MinIO 等镜像和依赖，慢是正常的。后续构建会使用缓存，速度会快很多。

### 15.2 前端能打开，但接口请求失败

查看前端和后端日志：

```bash
docker logs -f interview-frontend
docker logs -f interview-app
```

生产环境前端请求 `/api`，由前端容器里的 Nginx 转发到 `app:8080`。

### 15.3 后端连接数据库失败

检查 PostgreSQL：

```bash
docker compose ps postgres
docker logs interview-postgres
```

确认 `.env` 中 `POSTGRES_PASSWORD` 和容器启动时使用的密码一致。

### 15.4 AI 调用失败

常见原因：

- `.env` 没有配置 `AI_BAILIAN_API_KEY`。
- 系统设置里没有配置默认模型。
- 普通用户没有配置自己的 Provider 和默认模型。
- 服务器无法访问模型服务地址。

查看后端日志：

```bash
docker logs -f interview-app
```

### 15.5 文件上传失败

检查 MinIO 和 bucket 初始化任务：

```bash
docker compose ps minio createbuckets
docker logs interview-minio
```

项目默认 bucket：

```text
interview-guide
```
